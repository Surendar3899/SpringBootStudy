# Spring application name and port
spring.application.name=JPA-Project
server.port=8083

# H2 Database configuration
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=MYSQL
spring.datasource.driver-class-name=org.h2.Driver

spring.mvc.view.prefix=/views/
spring.mvc.view.suffix=.jsp


#spring.datasource.username=sa
#spring.datasource.password=

# Enable H2 console
spring.h2.console.enabled=true
#spring.h2.console.path=/h2-console

# JPA configuration
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true
#spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Disable Open-View for JPA transactions
spring.jpa.open-in-view=false

# Optional: If you need to set specific HikariCP pool settings, you can include them, but they are usually not required.
# spring.datasource.hikari.pool-name=HikariPool-1
# spring.datasource.hikari.minimum-idle=5
# spring.datasource.hikari.maximum-pool-size=10
# spring.datasource.hikari.auto-commit=true
# spring.datasource.hikari.connection-timeout=30000
# spring.datasource.hikari.idle-timeout=600000
# spring.datasource.hikari.max-lifetime=1800000
